{% extends 'base.html.twig' %}

{% block title %}Whishliste{% endblock %}

{% block body %}
    <h1>Details About your Whishliste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>numero</th>
                <td>{{ whishliste.id }}</td>
            </tr>
{# ajoute du titre dans show#}
              <tr>
                <th>Produits</th>
                <td>
                    {% for item in whishliste.Items %}
                        {{ item.titre }}{% if not loop.last %}, {% endif %}
                    {% else %}
                        Aucun produit ajouté.
                    {% endfor %}
                </td>
            </tr>

        </tbody>
    </table>


    <a href="{{ path('app_whishliste_index') }}"class="btn btn-primary btn-sm">back to list</a>

    <a href="{{ path('app_whishliste_edit', {'id': whishliste.id}) }}"class="btn btn-primary btn-sm">edit</a>

    {{ include('whishliste/_delete_form.html.twig') }}
{% endblock %}
