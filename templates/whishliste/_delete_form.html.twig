<div style="margin-bottom: 100px;">
{# lena juste bech nba3ed button 3al footer #}

<form method="post" action="{{ path('app_whishliste_delete', {'id': whishliste.id}) }}" 
onsubmit="return confirm('Are you sure you want to delete this item?');">

    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ whishliste.id) }}">
    
    <button class="btn btn-danger btn-sm w-40 mt-2" 
    style="background-color: #dc3545; color: white; border: none;">Delete</button>
       
</form>
</div>
